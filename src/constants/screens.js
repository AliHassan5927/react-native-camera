import { Platform } from "react-native";
import { colors } from ".";
import { height } from 'react-native-dimension'
export const SCREEN = {
    // Routes
    appStack: 'appStack',

    // AppNavigation
    home: 'home',
    previewImg: 'previewImg'

}
